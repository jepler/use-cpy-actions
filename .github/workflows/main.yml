on: [push]
jobs:
    job0:
        runs-on: ubuntu-latest
        name: do a thing, in a container
        steps:
            - name: step
              uses: jepler/circuitpython-actions-build@HEAD
              with:
                run: uname -a
